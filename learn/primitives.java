public class primitives {
    public static void main(String[] args) {
        int integer=64;
        char character='a';
        float floating_number=45.78f; //by default they are in double if we want to store in float we need to add f
        double big_float_number=1223344.456;
        long big_integer_number=123554755l;//by default they are in int so we add l
        boolean bool=true;
        System.out.println(integer+" "+floating_number);
    }
}
